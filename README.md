# crawl-data-app

Activate Your Virtual Environment
If you haven't already created a virtual environment, do so first:

On Windows:

python -m venv .venv
Then activate it:

.venv\Scripts\activate
On macOS/Linux:

python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt
